# Web3Forms access key used by the contact form server action.
# Create a key at https://web3forms.com (free) and restrict it to your domain
# in their dashboard. Without this set, the contact form returns a friendly
# "temporarily unavailable" message instead of sending.
WEB3FORMS_ACCESS_KEY=
