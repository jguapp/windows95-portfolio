# @vercel/analytics declares optional peer deps on every framework it supports
# (Remix, SvelteKit, Vue, Next). With auto-install-peers on, pnpm pulls the
# whole Svelte/Vue/Remix toolchain into the tree — hundreds of packages this
# Next app never imports, and the source of most `pnpm audit` findings.
auto-install-peers=false
